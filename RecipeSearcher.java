package com.infoshareacademy;

public class RecipeSearcher(){

      /*  List<Recipe> recipe;


        public List<Recipe> findRecipeByCategory(Category lookedCategory) {
        List<Recipe> recipesWithLookedCategory = new ArrayList<>();
        for(Recipe recipe : recipesList) {
        Category recipeCategory = recipe.getCategory();
        if(recipeCategory.equals(lookedCategory) {
        recipesWithLookedCategory.add(recipe);
        }
        }
        return recipesWithLookedCategory;
        }
        }*/
}